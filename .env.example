# Google AI Studio API Key (Gemini)
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Preview PIN Hash (optional, for staging gate)
# Generate with: node -e "console.log(require('crypto').createHash('sha256').update('YOUR_PIN','utf8').digest('hex'))"
VITE_PREVIEW_PIN_HASH=

# Project Passkey Hash (optional, for project details gate)
# Generate with: node -e "console.log(require('crypto').createHash('sha256').update('YOUR_PASSKEY','utf8').digest('hex'))"
VITE_PROJECT_PASSKEY_HASH=